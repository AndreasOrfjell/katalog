# Catalog Web (Vite + React)

## Quick start
```bash
cd web
npm install
npm run dev
```
Open http://localhost:5173 – dev‑server proxier `/api/*` til backend på `http://localhost:4000`.

## Bygg
```bash
npm run build
npm run preview
```
