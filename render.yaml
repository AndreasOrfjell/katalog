services:
  - type: web
    name: katalog
    runtime: docker
    plan: free
    autoDeploy: true
    region: frankfurt
    healthCheckPath: /api/health
    envVars:
      - key: MOCK_MODE
        value: "true"
      - key: EASYCONNECT_BASE_URL
        value: https://api.easyconnect.no
      - key: EASYCONNECT_API_KEY
        sync: false
